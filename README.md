1. Clone the app into your local environment
2. Create a virtual python environment in your local machine and navigate to it
3. Run 'pip install -r requirements.txt'
4. Then run 'npm i'
5. Run npm start, enjoy
